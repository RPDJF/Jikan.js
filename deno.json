{
    "tasks": {
        "test": "deno task check && deno test --allow-net --allow-env --allow-read --allow-write --trace-leaks test/main.test.ts",
        "check": "deno check src/**/*.ts ; deno lint src/**/*.ts"
    }
}